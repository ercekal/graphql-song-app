import React from 'react';
// import {useParams} from "react-router-dom";

const SongDetail = () => {
  // let { id } = useParams();

  return (
    <div>
      Song
    </div>
  );
};

export default SongDetail;